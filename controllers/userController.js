const express = require("express");
const router = express.Router();
const User = require("../models/Produit"); // Import your Mongoose model

// Define routes and controller functions here

module.exports = router;
